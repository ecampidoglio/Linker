dist: trusty
language: csharp
install: true
mono: none
dotnet: 2.1.500
sudo: required
branches:
  only:
    - master
script:
  - ./build.sh --target=Deploy
env:
  - DeployTo="https://linker-demo.scm.azurewebsites.net/api/zipdeploy"
